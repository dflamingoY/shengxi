<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginTop="10dp"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">

    <android.support.v7.widget.CardView
        android:id="@+id/cardLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:cardBackgroundColor="#00ffffff"
        app:cardCornerRadius="5dp"
        app:cardElevation="0dp">

        <ImageView
            android:id="@+id/iv_img"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <View
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/shape_img_layer" />

        <TextView
            android:id="@+id/view_Stroke"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:text="+"
            android:gravity="center"
            android:visibility="gone"
            android:textSize="19sp"
            android:textColor="@color/colorTextGray"
            android:background="@drawable/shape_r_5_stroke_b2b2b2" />

    </android.support.v7.widget.CardView>

    <ImageView
        android:id="@+id/iv_Delete"
        android:layout_width="21dp"
        android:layout_height="21dp"
        android:layout_alignTop="@+id/cardLayout"
        android:layout_alignRight="@+id/cardLayout"
        android:elevation="2dp"
        android:padding="3dp"
        android:src="@mipmap/icon_delete_theme_cover"
        android:visibility="gone" />

</RelativeLayout>